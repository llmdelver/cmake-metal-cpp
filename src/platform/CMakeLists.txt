set(PLATFORM_HEADER "")

if(APPLE)
    # metal-cpp header
    set(METAL_CPP_DIR ${CMAKE_CURRENT_SOURCE_DIR}/metal-cpp)

    # set(METAL_CPP_FOUNDATION_DIR ${METAL_CPP_DIR}/Foundation)
    # set(METAL_CPP_METAL_DIR ${METAL_CPP_DIR}/Metal)
    # set(METAL_CPP_QUARTZCORE_DIR ${METAL_CPP_DIR}/Metal)
    list(APPEND PLATFORM_HEADER ${METAL_CPP_DIR})
    set(PLATFORM_COMPILE_DEFINITION USE_METAL=1 PARENT_SCOPE)
endif()

set(PLATFORM_HEADER ${PLATFORM_HEADER} PARENT_SCOPE)